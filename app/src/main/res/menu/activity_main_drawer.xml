<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:title="@string/general_menu_group">
        <menu>
            <item
                android:id="@+id/active_alarms_menu"
                android:title="@string/active_notifications_list"
                android:icon="@drawable/table" />

            <item 
                android:title="@string/google_drive"
                android:id="@+id/google_drive"
                android:icon="@drawable/cloud" />

            <item
                android:id="@+id/calendar_import_menu"
                android:title="@string/import_calendar"
                android:icon="@drawable/calendar" />

            <item
                android:id="@+id/hot_points_menu"
                android:title="@string/hot_points"
                android:icon="@android:drawable/ic_dialog_map" />
        </menu>
    </item>

    <item android:title="@string/others_menu_group">
        <menu>

            <item
                android:id="@+id/info_menu"
                android:title="@string/info_menu"
                android:icon="@drawable/info" />

        </menu>
    </item>
</menu>
